/**
 * @param {Object} [options={}]
 */
const ExposedDefines = {
  IS_WEBPACKED,

  VERSION,
  DEVELOPMENT,
  PRODUCTION,

  GIT_VERSION,
  GIT_COMMIT_HASH,
  GIT_BRANCH,
  GIT_LAST_COMMIT_DATETIME
}

export default ExposedDefines