
import LOCATIONS from '../environment-locations-constants';
import STATES from '../environment-states-constants';

export {
  LOCATIONS,
  STATES,
}

const ENVIRONMENT = {
  LOCATIONS,
  STATES,
}

export default ENVIRONMENT;
